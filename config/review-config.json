{
  "maxPRSize": 500,
  "excludedFiles": [
    "package-lock.json",
    "yarn.lock",
    "pnpm-lock.yaml",
    "*.min.js",
    "*.min.css",
    "dist/**",
    "build/**",
    "node_modules/**",
    "*.svg",
    "*.png",
    "*.jpg",
    "*.jpeg",
    "*.gif",
    "*.ico",
    "*.woff",
    "*.woff2",
    "*.ttf",
    "*.eot"
  ],
  "reviewFocusAreas": {
    "security": true,
    "bugs": true,
    "codeQuality": true,
    "performance": true,
    "bestPractices": false
  },
  "severityThresholds": {
    "critical": true,
    "high": false,
    "medium": false,
    "low": false
  },
  "claudeModel": "claude-sonnet-4-6",
  "maxTokens": 8000,
  "codingStandards": {
    "enabled": true,
    "paradigm": "Clean, readable code with clear separation of concerns",
    "rules": [
      "No magic numbers or hardcoded secrets",
      "Error handling must be explicit — no silent catches",
      "Use meaningful variable and function names"
    ],
    "languageRules": {
      "JavaScript": [
        "Use const by default, let only when needed, never var",
        "Use strict equality (===) instead of loose equality (==)"
      ],
      "TypeScript": [
        "All functions should have explicit return types",
        "Avoid using 'any' type"
      ],
      "Python": [
        "Follow PEP 8 style guide",
        "Use type hints for function parameters and return types"
      ]
    }
  }
}
